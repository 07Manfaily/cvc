import React, { useState } from "react";
import { useParams } from "react-router-dom";
import { Grid, Button, Box, Tab, Typography } from '@mui/material';
import Header from '../components/header';
import userImage from '../assets/user.png'

export default function Credapp() {
   const { id } = useParams();
   return (
      <>
         <Header />
         <Grid container spacing={2} direction="column" sx={{ backgroundColor: "#CFCFCF" }}>
            <Grid item sx={{ mb: 2, ml: 2, mr: 2 }}>
               <Grid container sx={{ backgroundColor: "#2D3A23 ", color: "white", borderRadius: 7 }} direction="row" justifyContent="space-between" alignItems="center">
                  <Grid container justifyContent="space-between" alignItems="flex-start">
                     <img src={userImage} width={150} alt="user" />
                     <Grid item direction="column" justifyContent="space-between" alignItems="flex-start">

                        <Typography variant="h6" >Identifiant : {id}</Typography>
                        <Typography variant="h6">Yao Manfaily</Typography>
                     </Grid>
                     <Grid item direction="column" justifyContent="space-between" alignItems="flex-start">

                        <Typography variant="h6" >Identifiant : {id}</Typography>
                        <Typography variant="h6">Yao Manfaily</Typography>
                     </Grid>
                  </Grid>

               </Grid>
            </Grid>
         </Grid>
      </>
   )
}
